import 'package:flutter/material.dart';

class PatientContacts extends StatelessWidget {
  const PatientContacts({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      // list of doctors
      // list of caregivers
      // list of family
    );
  }
}